# Baza danych (lokalne dane, regenerowana przez /api/refresh)
articles.db
*.db
*.sqlite
*.sqlite3

# Klucze RSA (prywatny NIE w repo — tylko u autora)
keys/
.unlock

# Losowy identyfikator przeglądarki (generowany per-instalacja)
browser_identity.txt

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
venv-win7/
env/

# Build (PyInstaller / NSIS) — artefakty kompilacji
build/
dist/

# Tooling IJFW (lokalna pamięć/wspomaganie)
.ijfw/
ijfw/

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db
